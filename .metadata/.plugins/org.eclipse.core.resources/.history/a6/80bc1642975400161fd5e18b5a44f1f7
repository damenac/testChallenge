package root;

import java.util.List;

public class BucketSort implements ISort{

	@Override
	public void sort(List<Integer> numbers) {

		// Computers the max value in the array
		int max = -1;
		for (Integer integer : numbers) {
			if(integer > max)
				max = integer.intValue();
		}
		
	}

}
